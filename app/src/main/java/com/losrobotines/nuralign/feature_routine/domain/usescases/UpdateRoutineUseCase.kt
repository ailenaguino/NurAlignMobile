package com.losrobotines.nuralign.feature_routine.domain.usescases

import com.losrobotines.nuralign.feature_routine.data.RoutineRepositoryDatabase
import com.losrobotines.nuralign.feature_routine.presentation.RoutineViewModel
import javax.inject.Inject

/*
class UpdateRoutineUseCase @Inject constructor(
    private val routineRepository: RoutineRepositoryDatabase,
    private val loadRoutineUseCase: LoadRoutineUseCase,
    private val saveRoutineUseCase: SaveRoutineUseCase
) {

    suspend operator fun invoke(
        bedTime: String,
        activity: String,
        activityDays: List<String>,
        activityTime: String
    ) {
        val routine = loadRoutineUseCase()

        routine.sleepTime = bedTime
        routine.activity = activity
        routine.activityTime = activityTime
        routine.activityDays = activityDays

        saveRoutineUseCase(routine)
    }
}


 */

/*
class UpdateRoutineUseCase @Inject constructor(
    private val routineRepository: RoutineRepositoryDatabase,
    private val loadRoutineUseCase: LoadRoutineUseCase,
    private val saveRoutineUseCase: SaveRoutineUseCase
) {

    suspend operator fun invoke(
        bedTime: String,
        activity: String,
        activityDays: List<String>,
        activityTime: String
    ) {
        val routine = loadRoutineUseCase()

        routine.sleepTime = bedTime
        routine.activity = activity
        routine.activityTime = activityTime
        routine.activityDays = activityDays


        saveRoutineUseCase(routine)


    }


}

 */